# Чеклист до первого релиза (Desktop)

Делать **строго по порядку**.

---

## 0. Что уже готово

- `electron-updater` подключён, `publish: [{ provider: "github" }]` в `electron-builder.json`
- `autoUpdater.checkForUpdatesAndNotify()` в `main.js` только при `app.isPackaged`
- `electron:build` делает build → prep → electron-builder
- Prep policy asserts (sqlite/aws/gcs/cache/size) зелёные

---

## 1. GitHub Secrets (обязательно для подписи/нотаризации)

**Settings → Secrets and variables → Actions → New repository secret**

### 1.1 macOS notarization и подпись

| Secret | Описание |
|--------|----------|
| `APPLE_ID` | Apple ID (email) |
| `APPLE_TEAM_ID` | Team ID (10 символов) |
| `APPLE_APP_SPECIFIC_PASSWORD` | App-specific password (notarization) |
| `MAC_CERTS_P12` | **base64** сертификата Developer ID Application (.p12) |
| `MAC_CERTS_P12_PASSWORD` | Пароль от .p12 |

Без `MAC_CERTS_P12` / `MAC_CERTS_P12_PASSWORD` сборка mac пройдёт, но приложение будет неподписанным (и notarization не пройдёт). Для installer можно добавить второй p12 (Developer ID Installer) — не критично для старта.

**Notarization** запускается только когда заданы **и** `MAC_CERTS_P12` (и пароль), **и** все три `APPLE_*` env. Иначе сборка будет signed-only (подпись без нотаризации) или unsigned; workflow при этом не падает.

### 1.2 Windows code signing

| Secret | Описание |
|--------|----------|
| `WIN_CERT_PFX_BASE64` | **base64** сертификата .pfx |
| `WIN_CERT_PFX_PASSWORD` | Пароль от .pfx |

Без них Windows-сборка пройдёт, но SmartScreen будет ругаться.

### 1.3 GitHub token

`GITHUB_TOKEN` выдаётся автоматически в Actions, отдельный secret не нужен (если публикуете в тот же репо).

---

## 2. electron-builder.json

Проверить наличие:

- `publish: [{ "provider": "github" }]`
- `mac`: `target: ["dmg"]`, `hardenedRuntime: true`, `entitlements` / `entitlementsInherit`, `gatekeeperAssess: false`
- `win`: `target: "nsis"`

Остальное (asar, compression, files, extraResources и т.д.) оставить как есть.

---

## 🔎 Smoke Test перед релизом (обязательно)

Перед публикацией **любого desktop-релиза** необходимо выполнить проверки из файла:

**[DESKTOP_SMOKE_TEST.md](DESKTOP_SMOKE_TEST.md)**

Использовать режим:

- **Обычный релиз** → *Полный Smoke Test (5–7 минут)*
- **Hotfix / patch-релиз** → *Hotfix Smoke Test (2–3 минуты)*

> CI проверяет размер, cache и policy,
> но **UI / login / auto-update smoke** подтверждаются только этим чеклистом.

---

## 3. Release workflow

**Файл:** `.github/workflows/release.yml`

- **Триггер:** теги `v*.*.*` (например `v1.0.0`).
- **Job build-mac:** checkout → node → npm ci → Import macOS certs (если заданы `MAC_CERTS_P12` / `MAC_CERTS_P12_PASSWORD`) → Build (build + prep + next-cache-check + electron-builder --mac) → upload-artifact dist-mac.
- **Job build-win:** checkout → node → npm ci → Build (build + prep + next-cache-check; при наличии `WIN_CERT_PFX_*` — decode pfx и `CSC_LINK`/`CSC_KEY_PASSWORD`) → electron-builder --win → upload-artifact dist-win.
- **Job publish:** download artifacts (dist-mac, dist-win) → создать release по тегу (если нет) → загрузить **все** файлы из artifacts в Release (в т.ч. `latest-mac.yml`, `latest.yml`, `*.blockmap` для electron-updater).
- **Проверить после релиза:** в GitHub Release есть `latest*.yml` и `*.blockmap` — иначе auto-update не увидит обновление.

Если секретов macOS/Windows нет — сборка mac/win идёт без подписи; релиз и загрузка артефактов в Release всё равно выполняются.

---

## 4. Финальная проверка перед v1.0.0 (5 минут)

Локальный прогон перед первым релизом:

1. **Prod-режим Electron** — приложение стартует и работает:
   ```bash
   rm -rf electron/bundle
   DATABASE_URL="postgresql://user:pass@localhost:5432/db" S3_HEALTH_ENABLED=0 GCS_HEALTH_ENABLED=0 npm run electron:prod
   ```

2. **Сборка установщика** — `electron:build` проходит (в т.ч. next-cache-check):
   ```bash
   rm -rf electron/bundle
   DATABASE_URL="postgresql://user:pass@localhost:5432/db" S3_HEALTH_ENABLED=0 GCS_HEALTH_ENABLED=0 npm run electron:build
   ```

3. **Config.env UX** — открыть `/system/settings`, сохранить изменения, restart; убедиться, что `userData/config.env` обновился и env применяется.

---

## 5. Первый релиз v1.0.0 (команды)

1. Убедиться, что в `package.json` указано `"version": "1.0.0"`.
2. Запушить `main`.
3. Создать и запушить тег:

```bash
git tag v1.0.0
git push origin v1.0.0
```

После пуша тега запустится workflow **release**: соберёт mac и win, подпишет/нотаризует (если secrets заданы), зальёт артефакты в GitHub Release `v1.0.0`.

**Проверить после релиза:** в Release должны быть загружены не только `.dmg` и `.exe`, но и **`latest-mac.yml`**, **`latest.yml`**, **`*.blockmap`** — без них auto-update не увидит обновление.

**Smoke Test перед публикацией:** скачать `.dmg`/`.exe` из Release, установить и пройти [DESKTOP_SMOKE_TEST.md](DESKTOP_SMOKE_TEST.md) (~5–7 мин).

---

## 6. Проверка auto-update (после первого релиза)

1. Скачать и установить `v1.0.0` (dmg / exe из Release).
2. Сделать новый релиз: bump version на `1.0.1`, тег `v1.0.1`, push.
3. Установленная `v1.0.0` при запуске (или через время) должна через `checkForUpdatesAndNotify()` увидеть обновление и предложить установку.

Для «нормального» апдейта на macOS и Windows подпись желательна.

---

## 7. Если сертификатов пока нет

- Workflow **release** будет собирать mac и win без подписи (шаг Import certs и Windows CSC пропускаются при пустых секретах).
- Релиз и загрузка в GitHub Release выполняются; приложение будет работать, но ОС может показывать предупреждения.

Когда появятся Apple Developer ID и/или Windows code signing cert — добавить секреты по п. 1; перезапуск релиза по тому же тегу перезапишет артефакты при необходимости (или создать новый тег).

---

## Реализованные доработки

| Функция | Где |
|--------|-----|
| **Nightly / prerelease** | Теги `v*.*.*-*` (alpha/beta/nightly) → [release-prerelease.yml](../../.github/workflows/release-prerelease.yml). Версия подставляется из тега; релиз создаётся как prerelease (stable не видит). В main.js канал выставляется по версии (`-alpha`/`-beta`/`-nightly` → `allowPrerelease`). |
| **Delta-updates (NSIS)** | В `electron-builder.json`: `nsis.differentialPackage: true` — Windows-обновления по delta. |
| **Crash-reporting (Sentry)** | `@sentry/electron`: инициализация в main.js при `SENTRY_DSN` в config.env (только packaged). Шаблон в env.production.example. |
| **Метрики релизов** | В release.yml и release-prerelease.yml: шаг «Release metrics (build size)» — размер артефактов в лог. Update success — лог `update-downloaded` в main.js. |

---

## Операционное развитие (не техническое)

Следующий шаг — **операционный**, а не доработка кода:

| Направление | Содержание |
|-------------|------------|
| **Dashboard по Sentry** | Настроить дашборд в Sentry: краши по версии, по ОС, тренды. Использовать для мониторинга после каждого релиза. |
| **Решения по crash rate / update success** | На основе дашборда: пороги (crash rate, доля успешных обновлений), критерии «откатить / патчить / выпускать дальше». Зафиксировать в runbook или политике. |
| **Пользовательский канал beta** | По желанию: дать части пользователей возможность перейти на beta (установка с тегом `v*.*.*-beta.*` или отдельная сборка). Решения по релизу в stable — с учётом метрик beta. |

---

## Возможные доработки (бэклог)

| Идея | Назначение |
|------|------------|
| **Telemetry** | Расширить: отправка размера/успеха обновления в бэкенд или Sentry breadcrumbs. |

---

## Чего не нужно делать сейчас

- Дальше оптимизировать размер (policy уже всё защитила).
- Усложнять preload/IPC.
- Трогать .next (policy уже всё защитила).
