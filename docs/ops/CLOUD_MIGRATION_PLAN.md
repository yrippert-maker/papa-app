# Cloud Migration Plan — Summary & Remaining Steps

## Текущий статус (выполнено)

Проект полностью подготовлен к облачной эксплуатации:

* База данных перенесена в облако (Supabase Postgres), подключение через **Session Pooler**, SSL настроен корректно.
* Prisma 7 настроена через `prisma.config.ts`, миграции применяются и контролируются.
* Реализован **RBAC**:

  * роли `admin / user / auditor`,
  * роли прокидываются в session (NextAuth),
  * middleware защищает страницы,
  * guards защищают API и server actions,
  * инварианты ролей (разрешённые роли, default role, запрет удаления последнего admin).
* Реализован **Audit Log**:

  * единый формат `domain.verb`,
  * расширенная metadata (requestId, ip, userAgent, method, path, targetId, diff),
  * логирование логина и административных действий,
  * UI для просмотра аудита с фильтрами,
  * индексы под UI-фильтры,
  * политика retention (по умолчанию 180 дней) и скрипты prune/dry-run.
* Добавлена **наблюдаемость**:

  * сквозной `requestId`,
  * корреляция audit ↔ API.
* Добавлены **тесты**:

  * unit для guards,
  * API tests,
  * e2e Playwright для RBAC middleware.
* Подготовлены **операционные процедуры**:

  * миграции (`migrate deploy`, `status`),
  * backup/restore (`pg_dump` / `pg_restore`),
  * PROD_RUNBOOK.md с dry-run восстановлением.

На этом этапе система считается **архитектурно и операционно готовой**.

---

## Оставшиеся шаги (критический путь)

### Шаг 2 — Deploy backend (критический)

Цель: приложение стабильно работает в облаке без пользовательского трафика.

**Рекомендуемый путь:** Railway — см. `docs/ops/RAILWAY_DEPLOY.md`.

Задачи:

* Создать production-окружение (secrets, ENV).
* Указать `DATABASE_URL` (Session pooler или Direct + IPv4 add-on при необходимости).
* Выполнить `npm run db:migrate:prod`.
* Проверить:

  * health endpoint,
  * login через NextAuth,
  * запись `AuditEvent`,
  * корректную работу RBAC.
* Убедиться, что `requestId` проходит через middleware → API → audit.

Критерий готовности: backend доступен, логин и аудит работают.

---

### Шаг 3 — Перенос файлов / статики

Цель: все пользовательские и служебные файлы доступны в новом окружении.

Задачи:

* Инвентаризация файлов (uploads, exports, snapshots).
* Выбор и настройка хранилища (S3 / Supabase Storage / R2).
* Миграция файлов.
* Проверка прав доступа и ссылок.
* При необходимости — логирование события `storage.migrate`.

Критерий готовности: файлы доступны и соответствуют старому окружению.

---

### Шаг 4 — Prod domain и auth

Цель: пользователи работают через финальный домен.

Задачи:

* Настройка DNS и HTTPS.
* Настройка NextAuth:

  * `NEXTAUTH_URL`,
  * callback URLs,
  * cookie flags (`secure`, `sameSite`).
* Проверка login/logout, redirect'ов, cookies.

Критерий готовности: аутентификация корректно работает с прод-домена.

---

### Шаг 5 — Cutover / Go-live

Цель: окончательное переключение пользователей.

Задачи:

* Перевести старую БД в read-only (если применимо).
* Финальный backup.
* Выполнить smoke-проверки из PROD_RUNBOOK.
* Мониторинг ошибок, auth, audit.
* Подготовленный rollback-план.

Критерий готовности: пользователи работают в новом окружении без инцидентов.

---

## Дополнительные (неблокирующие) задачи

* Ротация database credentials.
* Автоматизация audit retention (cron/job).
* Alerting (auth failures, 403 spikes).
* Периодический review администраторов и ролей.
* Проверка восстановления из backup на отдельном проекте.
* **Если роли начнут меняться через Prisma UserRole** — добавить триггер `prevent_last_admin_*` на join-таблицу + обновить guards.

---

## Итог

На текущем этапе:

* архитектура завершена,
* безопасность и аудит реализованы,
* операционные сценарии описаны,
* система готова к деплою и эксплуатации.

Оставшиеся шаги носят **инфраструктурный и организационный характер** и не требуют изменений в доменной логике приложения.

---
Коротко и по делу: **ты вышел из фазы "разработка и миграция" и вошёл в фазу "эксплуатация и рост"**.
Дальше выбор уже не технический, а управленческий. Вот **3 логичных траектории**, из которых обычно выбирают одну главную (остальные идут фоном).

---

## Вариант 1. Go-Live без сюрпризов (самый правильный следующий шаг)

Если проект близок к реальным пользователям.

**Что делать дальше (по порядку):**

1. **Prod-деплой backend** (если ещё не сделан)
2. **Dry-run восстановления** (restore backup в тестовый проект)
3. **Cutover rehearsal**
   – прогнать шаги go-live "на сухую"
   – замерить время и точки отказа
4. **Минимальный мониторинг**

   * auth failures
   * 403 spikes
   * error rate API
5. **Чек-лист "первый день в проде"**

👉 Результат: проект готов к реальному использованию, без паники.

---

## Вариант 2. Эксплуатация как продукт (если проект будет жить долго)

Если это не MVP на пару недель.

**Фокус:**

* Observability:

  * error tracking (Sentry или аналог)
  * алерты по auth/audit
* Performance:

  * лимиты на audit/events API
  * pagination + кеш
* Data hygiene:

  * регулярный audit-prune
  * контроль роста таблиц
* Security:

  * periodic access review
  * rotation secrets

👉 Результат: система не деградирует через 3–6 месяцев.

---

## Вариант 3. Развитие функционала (если платформа — основа продукта)

Если инфраструктура готова и хочется идти в фичи.

Типичные следующие шаги:

* UI для управления ролями (RBAC admin panel)
* Расширение audit (export, compliance reports)
* Domain-специфичные модули (то, ради чего всё это делалось)
* API для интеграций

👉 Результат: рост ценности продукта, а не только "правильная архитектура".

---

## Моя рекомендация (если не выбирать самому)

**Сначала Вариант 1 → затем Вариант 2 → потом Вариант 3.**

Ты уже проделал редкую работу:

> сначала **устойчивость**, потом **фичи**, а не наоборот.
