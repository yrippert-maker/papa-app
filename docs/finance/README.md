# Финансы — реестр платежей

Для Mail MVP здесь хранится **единый реестр платежей**, обновляемый после одобрения оператором (Accept + safe_auto или draft).

- **Формат (MVP):** `payments.json` или `payments.csv` — см. [MAIL_MVP_SPEC](../plans/MAIL_MVP_SPEC.md) раздел A3.
- **Правила:** только добавление новых строк (append), без редактирования истории.

Файл реестра будет создан при реализации Sprint M2.
