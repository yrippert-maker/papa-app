# Bundle Fingerprint (шаблон)

**Назначение:** шаблон для `BUNDLE_FINGERPRINT.md`, генерируемого скриптом `create-regulatory-bundle.sh`.

Скрипт создаёт актуальный `BUNDLE_FINGERPRINT.md` при сборке. См. [REGULATORY_BUNDLE_MANIFEST.md](REGULATORY_BUNDLE_MANIFEST.md).
